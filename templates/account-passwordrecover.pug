extends layout.pug

block content
  .col-lg-6.col-lg-offset-3.col-md-6.col-md-offset-3
    h3 Восстановить пароль
    if recovered
      .alert.alert-success.center.messagebox
        strong Ваш пароль был изменен
        p Вашему аккаунту был присвоен временный пароль <code>#{recoverPw}</code>.  Теперь вы можете использовать этот пароль для входа в систему и выбрать новый пароль, посетив страницу <a href="/account/edit">смены пароля / email.</a>
    else if confirm
      form(role="form", method="POST")
        button.btn.btn-primary.btn-block(type="submit") Нажмите, чтобы сбросить пароль
    else
      .alert.alert-danger.center.messagebox
        strong Не удалось восстановить пароль
        p= recoverErr
