extends layout.pug

block content
  .col-lg-6.col-lg-offset-3.col-md-6.col-md-offset-3
    h3 Восстановить пароль
    if reset
      .alert.alert-success.center.messagebox
        strong Отправлен запрос на сброс пароля
        p На ваш email отправлена ссылка для восстановления пароля.
    else if resetErr
      .alert.alert-danger.center.messagebox
        strong Ошибка
        p= resetErr
    form(action="/account/passwordreset", method="post", role="form")
      input(type="hidden", name="_csrf", value=csrfToken)
      .form-group
        label.control-label(for="username") Логин
        input#username.form-control(type="text", name="name")
      .form-group
        label.control-label(for="email") Email адрес
        input#email.form-control(type="email", name="email")
      button.btn.btn-primary.btn-block(type="submit") Отправить запрос на сброс пароля
